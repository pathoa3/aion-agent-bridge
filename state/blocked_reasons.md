# Blocked reasons

Current corpus does not contain enough code-side evidence to decode the sample capture.

Clean blocker:

```text
blocked_need_new_code_evidence
```

Required next evidence:

1. file-backed EuroAion transform/key routine or modified public motif
2. concrete packet sink or decrypt callsite proven by code xref, not imports alone
3. unpacked/less-protected comparable build exposing the packet transform
4. exact documentation/source for the EuroAion 7785 packet transform/key schedule

Do not spend more cycles on:

- same PCAP extraction after Pass600
- public AionEmu/RageZone crypto already covered
- source candidates archived by Pass601
- normal `/say` capture loops
- import presence alone
