# Pass651 Extended S2C Receive Structure

Runs a multi-stage offline analysis of the current 7780/10242 length-ladder capture.

The stages produce only safe metadata in Git. Local payload inspection is limited to aggregate byte-class statistics under `C:\AionTools\aion_decoder_agent\outbox\pass651_local_payload_triage`; no raw payload bytes, ciphertext, decoded packet blobs, raw plaintext, packet hashes, derived keys, binaries, DLLs, EXEs, tokens, or secrets are written.
